Tools and Libraries
>Open Cv
>Numpy  A numpy array is a grid of values, all of the
 same type, and is indexed by a tuple of nonnegative integers.
 The number of dimensions is the rank of the array;
 the shape of an array is a tuple of integers giving the
 size of the array along each dimension.
>OS
>PILLOW=Python Imaging Library (abbreviated as PIL) 
(in newer versions known as Pillow) is a free library for the Python programming language 
that adds support for opening, manipulating, and saving many different image file forma

#HaarCascade
#LBPHF recognizer


Process
>Detecting face using classifier using opencv library for computer vision

>Creating a DataSet of a face from different angles

>Training

>Recognizing Face using a recognizer 